---
import { getEntry } from 'astro:content';
import Page from '../../../templates/Page.astro';

const entry = await getEntry('pages', 'en/work-photographer');
if (!entry) throw new Error('Entry en/work-photographer not found');
---

<Page entry={entry} />

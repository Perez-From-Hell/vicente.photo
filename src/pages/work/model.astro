---
import { getEntry } from 'astro:content';
import Page from '../../templates/Page.astro';

const entry = await getEntry('pages', 'es/work-model');
if (!entry) throw new Error('Entry es/work-model not found');
---

<Page entry={entry} />
